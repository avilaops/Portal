# Configuração de Produção - Avila Portal
# IMPORTANTE: Não commitar este arquivo com valores reais!

# Database (use Azure PostgreSQL, AWS RDS, etc)
DATABASE_URL=postgres://username:password@production-db.postgres.database.azure.com:5432/avila_portal?sslmode=require

# Redis (use Azure Cache for Redis, AWS ElastiCache, etc)
REDIS_URL=rediss://username:password@production-redis.redis.cache.windows.net:6380

# JWT Secret (gerar com: openssl rand -base64 64)
JWT_SECRET=CHANGE_THIS_IN_PRODUCTION_USE_STRONG_SECRET_KEY

# JWT Expiration (em segundos)
JWT_EXPIRATION=86400

# Server Config
HOST=0.0.0.0
PORT=8080

# Frontend URL (para CORS)
FRONTEND_URL=https://portal.avila.inc

# Logging
RUST_LOG=info
RUST_BACKTRACE=1

# Opcional: Application Insights / DataDog / New Relic
# APPINSIGHTS_INSTRUMENTATIONKEY=your-key
# DD_API_KEY=your-key
# NEW_RELIC_LICENSE_KEY=your-key
